from __future__ import annotations

import os
from pathlib import Path

# Base folder for NullShift data
HOME_FOLDER = Path.home() / ".nullshift"

# Log file location
LOG_FILE = HOME_FOLDER / "nullshift.log"
